/**
 * 💡 This is not a route.
 *
 * Only the `index.ts` file directly under the route folder (e.g. `/api.users.[id]/index.ts`) is a route.
 * Any other file — including files in subfolders — is a colocated module, not a route.
 */
